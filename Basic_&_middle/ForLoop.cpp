#include <iostream>

using namespace std;

int main()
{
    for (int i = 1; i <= 5; i++) { // Initialize i to 1, loop while i is less than or equal to 5, and increment i by 1 in each iteration
        cout << i << endl; // Print the current value of i
    }

    return 0;
}